# Core inference (GPU track)
torch>=2.6.0
# Gemma 4 is multimodal: Gemma4Processor -> image_processing_gemma4 imports
# torchvision.transforms.v2. Without it transformers reports the unhelpful
# "Could not import module 'Gemma4Processor'" rather than the real cause.
torchvision>=0.21.0
# Gemma 4 (model_type "gemma4", Gemma4Processor / Gemma4ForConditionalGeneration)
# landed in transformers 5.5.0. Anything older resolves the processor class to
# nothing and dies with "Could not import module 'Gemma4Processor'".
transformers>=5.5.0
accelerate>=1.2.0
safetensors>=0.4.5
sentencepiece>=0.2.0
pillow>=10.4.0

# Quantized GPU track (optional, for 8-bit / 4-bit single-GPU runs)
bitsandbytes>=0.45.0

# CPU track driver (llama.cpp is built from source; this is the HTTP client)
requests>=2.32.0

# Eval + reporting
sacrebleu>=2.4.0
numpy>=1.26.0
tabulate>=0.9.0
