# Core inference (GPU track)
torch>=2.6.0
transformers>=4.57.0
accelerate>=1.2.0
safetensors>=0.4.5
sentencepiece>=0.2.0
pillow>=10.4.0

# Quantized GPU track (optional, for 8-bit / 4-bit single-GPU runs)
bitsandbytes>=0.45.0

# CPU track driver (llama.cpp is built from source; this is the HTTP client)
requests>=2.32.0

# Eval + reporting
sacrebleu>=2.4.0
numpy>=1.26.0
tabulate>=0.9.0
